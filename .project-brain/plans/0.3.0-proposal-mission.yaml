schema_version: "2.5.0"
artifact_type: implementation-plan
objective: Harden, document, and independently review the standalone proposal command
repository_sha: 07e8d231add46dbe3e654ec6e11a79a1f59bdc8c
steps:
  - Verify mission, SHA, evidence, secret, fingerprint, duplicate, dry-run, structured-input, API, and CLI behavior.
  - Add missing public CLI and library documentation plus trust-boundary tests.
  - Run deterministic regression, packaging, skill, fixture, and lifecycle checks.
  - Obtain an independent review using a separate minimal context.
  - Resolve findings, close the mission, propose and evaluate one learning, and curate without promotion.
risks:
  - Mission artifacts can make the worktree dirty before mission closure; implementation commits must precede closure.
  - Stable proposal identity and fingerprint identity must not form an unstable circular dependency.
  - Structured input must not bypass evidence or secret validation.
validation:
  - python3 -m unittest discover -s tests -p 'test_*.py'
  - project-brain validate --repo .
  - python3 scripts/install_skill.py validate
  - byte-identical repeated evaluation regression
evidence:
  - kind: artifact
    reference: .project-brain/context-packs/0.3.0-proposal-mission-final.yaml
  - kind: file
    reference: src/project_brain/proposals.py
context_metrics:
  candidate_files: 90
  initial_candidate_bytes: 223062
  first_selected_bytes: 28608
  first_selected_files: 8
  missing_context_discovered:
    - src/project_brain/core.py
  final_selected_bytes: 88673
  final_selected_files: 9
  final_reduction_percentage: 60.25
