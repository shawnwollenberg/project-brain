# ADR 0004: Deterministic retrieval before semantic retrieval

Status: Accepted

Context selection uses explicit references, expected paths, metadata matches, bounded lexical matches, stable scores, and path tie-breaking. This makes selection explainable and budgeted. Semantic retrieval may later augment it explicitly but cannot silently replace it.
