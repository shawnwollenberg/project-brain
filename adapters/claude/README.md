# Claude Code adapter

Expose the repository's `AGENTS.md` guidance through the project's Claude instruction entry point by reference rather than duplication. Invoke the standalone CLI before and after work, retain structured findings under `.project-brain/`, cite repository evidence, and leave promotion to a different human reviewer.
