# Provider-neutral agent adapter

Before work, read repository `AGENTS.md`, run `project-brain prepare-context`, and use only the selected `.project-brain/` knowledge plus explicitly requested files. After work, record a mission result with inspectable evidence and create learnings only as proposals. Review findings should use the packaged `review-findings` schema. Never self-approve.
