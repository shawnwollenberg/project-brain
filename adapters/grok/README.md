# Grok and external reviewer adapter

Provide an external reviewer with the generated context pack and relevant schema, not the entire knowledge tree. Require structured review findings with file/evidence references. Import proposed learnings as `proposed` only and require a human disposition; remote execution and provider APIs are out of scope.
